import numpy as np

# sizes = [2, 4, 3]
# bias = [np.random.randn(y, 1) for y in sizes[1:]]
# weights = [np.random.randn(y, x) for x, y in zip(sizes[:-1], sizes[1:])]
# print(bias)
# print(weights)

for x, y in zip([6, 5, 3], [1, 2]):
    print(x, y)
